info:
  name: Create
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/v1/todo/create"
  body:
    type: json
    data: |-
      {
        "nombre": "task 1",
        "descripcion": "task description",
        "prioridad": "alta"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
