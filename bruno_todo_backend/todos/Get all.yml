info:
  name: Get all
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/v1/todo/list"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
