info:
  name: Login
  type: http
  seq: 2

http:
  method: POST
  url: "{{url}}/v1/login"
  body:
    type: json
    data: |-
      {
        "email": "test@mail.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
