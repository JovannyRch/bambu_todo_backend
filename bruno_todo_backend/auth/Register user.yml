info:
  name: Register user
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/v1/register"
  body:
    type: json
    data: |-
      {
        "email": "test2@mail.com",
        "nombre": "usuario de prueba",
        "password": "123456"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
