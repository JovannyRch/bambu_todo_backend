info:
  name: Register user
  type: http
  seq: 1

http:
  method: POST
  url: "{{url}}/v1/register"
  body:
    type: json
    data: |-
      {
        "email": "test@mail.com",
        "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
